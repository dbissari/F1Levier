<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="20dp">

    <EditText
        android:id="@+id/edit_text_participant_name"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="@string/participant_name"
        android:inputType="text"
        android:singleLine="true" />

    <Spinner
        android:id="@+id/spinner_participant_level"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentStart="true"
        android:layout_below="@id/edit_text_participant_name"
        android:hint="@string/participant_level"/>

</RelativeLayout>